# Friday 设计语言

## 1. 设计原则

| 原则 | 含义 | 在 Friday 中的体现 |
|------|------|-------------------|
| **信息密度优先** | 开发者需要高效获取信息，不做无意义留白 | 诊断步骤紧凑排列，工具输出折叠/展开可控，日志默认紧凑视图 |
| **状态即视觉** | 系统状态通过颜色和图标一目了然 | 绿=运行中/正常，黄=需确认，红=错误/高风险，灰=已完成/已停止 |
| **流式透明** | Agent 的每一步思考、执行、结果都实时可见 | LLM 输出 token-by-token 流式渲染，工具执行有生命周期事件动画 |
| **暗色为本** | 开发者长时间使用，暗色模式是默认而非可选 | 纯黑基底（OLED 友好），Light Mode 作为后续可选项 |
| **可回溯** | 诊断是探索过程，用户需要随时回看历史 | 时间线布局，每步带时间戳，工具结果可展开回看 |

## 2. 色彩系统

### 语义色 Token

基于纯黑基底，通过三层级表面（surface-1/2/3）营造深邃感，CSS 变量 + `@theme inline` 映射到 Tailwind v4。

| Token | Hex | 用途 |
|-------|-----|------|
| `--background` | `#000000` | 全局背景（纯黑） |
| `--foreground` | `#E8E8E8` | 主文本 |
| `--surface-1` | `#0A0A0A` | 一级表面（顶栏、侧栏） |
| `--surface-2` | `#121212` | 二级表面（卡片、按钮） |
| `--surface-3` | `#1A1A1A` | 三级表面（hover 态） |
| `--card` | `#0D0D0D` | 卡片/面板背景 |
| `--card-foreground` | `#E8E8E8` | 卡片内文本 |
| `--primary` | `#121212` | 主操作面 |
| `--primary-foreground` | `#FFFFFF` | 主操作面文本 |
| `--secondary` | `#1A1A1A` | 次要操作面 |
| `--secondary-foreground` | `#E8E8E8` | 次要操作面文本 |
| `--muted` | `#121212` | 静默背景（输入框、禁用区） |
| `--muted-foreground` | `#6B6B6B` | 次要文本 |
| `--border` | `rgba(255,255,255,0.08)` | 边框/分隔线 |
| `--border-strong` | `rgba(255,255,255,0.14)` | 强边框（分隔线） |
| `--accent` | `#3B82F6` | 强调色：蓝（Blue-500） |
| `--accent-foreground` | `#FFFFFF` | 强调色上文本 |
| `--accent-glow` | `rgba(59,130,246,0.15)` | 强调色辉光 |
| `--destructive` | `#EF4444` | 错误/高风险（Red-500） |
| `--destructive-foreground` | `#FFFFFF` | 错误色上文本 |
| `--warning` | `#EAB308` | 警告/需确认（Yellow-500） |
| `--warning-foreground` | `#000000` | 警告色上文本 |
| `--success` | `#22C55E` | 成功（Green-500） |
| `--success-foreground` | `#000000` | 成功色上文本 |
| `--ring` | `#3B82F6` | 键盘焦点环 |

### 状态语义色

| 状态 | 颜色 | Token 映射 | 场景 |
|------|------|-----------|------|
| 运行中/成功 | 绿 `#22C55E` | `--success` | Agent 正在思考、工具执行中、诊断完成 |
| 需确认 | 黄 `#EAB308` | `--warning` | 低风险工具等待确认 |
| 高风险 | 红 `#EF4444` | `--destructive` | 高风险工具警告、错误、SSH 连接失败 |
| 已停止/已完成 | 灰 `#6B6B6B` | `--muted-foreground` | Agent 已停止、历史会话 |
| 信息/强调 | 蓝 `#3B82F6` | `--accent` / `--info` | 品牌强调、交互高亮、中性提示 |

### 对比度要求

| 元素 | 最低对比度 |
|------|-----------|
| 正文文本 | 4.5:1 |
| 大标题（≥18px bold） | 3:1 |
| 图标/非文本 UI | 3:1 |
| 焦点环 | 3:1 |
| 禁用状态 | 不要求，但需视觉可辨识 |

## 3. 排版

### 字体配对

| 角色 | 字体 | 字重 | 用途 |
|------|------|------|------|
| **标题/代码** | JetBrains Mono | 400 / 500 / 600 / 700 | 章节标题、工具名、命令、日志输出、Agent 思考 |
| **正文** | IBM Plex Sans | 300 / 400 / 500 / 600 / 700 | UI 文本、描述、按钮、表单 |

### 字号层级

| Token | 字号 | 行高 | 用途 |
|-------|------|------|------|
| `--text-xs` | 11px | 16px | 标签、时间戳、元信息 |
| `--text-sm` | 13px | 20px | 正文（紧凑）、工具参数 |
| `--text-base` | 14px | 24px | 正文（默认）、Agent 输出 |
| `--text-lg` | 16px | 28px | 卡片标题、步骤标题 |
| `--text-xl` | 20px | 28px | 页面标题 |
| `--text-2xl` | 24px | 32px | 会话标题 |

### CSS 导入

```css
@import url('https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500;600;700&display=swap');
```

## 4. 间距与布局

### 间距 Token（8dp 节奏）

| Token | 值 | 用途 |
|-------|----|------|
| `--space-1` | 4px | 图标内间距、紧凑元素 |
| `--space-2` | 8px | 组件内 padding、小间距 |
| `--space-3` | 12px | 列表项间距 |
| `--space-4` | 16px | 卡片 padding、默认间距 |
| `--space-5` | 20px | 中间距 |
| `--space-6` | 24px | 区块间距 |
| `--space-8` | 32px | 大区块间距 |
| `--space-10` | 40px | 页面级间距 |
| `--space-12` | 48px | 页面级间距 |
| `--space-16` | 64px | 大页面级间距 |

### 圆角

| Token | 值 | 用途 |
|-------|----|------|
| `--radius-sm` | 4px | 标签、小按钮 |
| `--radius-md` | 8px | 按钮、输入框 |
| `--radius-lg` | 12px | 卡片、面板 |
| `--radius-xl` | 16px | 大卡片、容器 |
| `--radius-full` | 9999px | 状态点、头像 |

### 主布局

```
┌──────────────────────────────────────────────────────────┐
│ 顶栏 (48px)                                               │
│ [F icon] Friday              [状态指示] [设置]            │
├──────────┬───────────────────────────────────────────────┤
│          │                                               │
│ 会话列表  │            主诊断区                           │
│ (240px)  │                                               │
│          │  ┌─────────────────────────────────────────┐  │
│ [会话1]  │  │ Agent 思考流 (流式渲染)                  │  │
│ [会话2]  │  │ ...                                      │  │
│ [会话3]  │  │ [工具执行卡片]                           │  │
│          │  │   jstat -gc 12345 1000 5                 │  │
│          │  │   [展开输出 ▸]                           │  │
│          │  │ ...                                      │  │
│          │  │ [确认请求卡片]                           │  │
│          │  │   ⚠ arthas trace 将注入字节码            │  │
│          │  │   [确认] [拒绝]                          │  │
│          │  └─────────────────────────────────────────┘  │
│          │                                               │
│          │  ┌─────────────────────────────────────────┐  │
│ [+ 新会话]│  │ 输入框                                   │  │
│          │  │ [补充信息发送]                            │  │
│          │  └─────────────────────────────────────────┘  │
└──────────┴───────────────────────────────────────────────┘
```

## 5. 组件设计

### 5.1 会话列表项

```
┌──────────────────────────┐
│ ● OOMService OOM 诊断     │  ← 状态点 + 标题
│   192.168.1.100 · 3步     │  ← 环境 + 进度
│   2 分钟前                 │  ← 时间
└──────────────────────────┘
```

- 状态点：绿(运行) / 灰(停止) / 黄(待确认) / 红(错误)
- 选中项：左侧 2px 绿色边条 + 背景提亮

### 5.2 Agent 思考流

- 逐 token 流式渲染（typewriter 效果），不用 spinner 等待完整响应
- 思考文本用 `JetBrains Mono`，`--muted-foreground` 色，区别于最终结论
- 最终结论用 `IBM Plex Sans`，`--foreground` 色，加粗

### 5.3 工具执行卡片

```
┌─────────────────────────────────────────────────┐
│ ▸ [jstat] -gc 12345 1000 5          ✓ 1.2s     │  ← 折叠态
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│ ▾ [jstat] -gc 12345 1000 5          ✓ 1.2s     │  ← 展开态
│   ┌─────────────────────────────────────────┐   │
│   │  S0C    S1C    ...  YGC   FGC  GCT      │   │
│   │  2048.0 2048.0 ...  12    0    0.234    │   │  ← 结构化输出
│   └─────────────────────────────────────────┘   │
│   [复制] [用作上下文]                             │
└─────────────────────────────────────────────────┘
```

- 工具名用 `JetBrains Mono`，绿色 badge
- 执行中：badge 显示 spinner + "执行中..."
- 成功：✓ + 耗时
- 失败：✗ + 错误摘要，展开看详情
- 输出区：等宽字体，可折叠，支持复制

### 5.4 确认请求卡片

**低风险（黄色边条）：**
```
┌─────────────────────────────────────────────────┐
││ ⚠ 需要确认: arthas trace                        │
││ 将注入字节码到目标 JVM，可能影响性能             │
││ [确认执行]  [拒绝]                              │
└─────────────────────────────────────────────────┘
```

**高风险（红色边条 + 警告底色）：**
```
┌─────────────────────────────────────────────────┐
││ ⚠⚠ 高风险操作: jmap -dump                      │
││ 此操作将触发 Full GC (STW)，可能导致服务暂停    │
││ 目标: 192.168.1.100 PID 12345                  │
││ [我已了解风险，确认执行]  [拒绝]                │
└─────────────────────────────────────────────────┘
```

### 5.5 输入框

- 底部固定，多行可展开
- Agent 运行时输入框提示"补充信息..."，发送后会追加到会话
- Agent 停止后提示"重新描述问题或补充信息..."
- 支持 `Shift+Enter` 换行，`Enter` 发送

### 5.6 状态指示器

| 状态 | 视觉 | 位置 |
|------|------|------|
| Agent 运行中 | 绿色脉冲点 + "诊断中" | 顶栏右侧 |
| Agent 已停止 | 灰色静态点 + "已停止" | 顶栏右侧 |
| 待确认 | 黄色闪烁点 + "待确认" | 顶栏右侧 |
| 连接中 | 蓝色 spinner + "连接环境" | 顶栏右侧 |
| 连接失败 | 红色点 + "连接失败" | 顶栏右侧 |

## 6. 图标

### App Icon

自定义 SVG 图标（`app-icon.svg`），用于窗口标题栏、任务栏、安装包。设计：纯黑圆角方块 + 白色几何 F + 蓝色 ECG 脉搏线延伸自 F 中横线，象征"诊断 + 监控"。

前端通过 `src/components/FridayMark.tsx` 内联 SVG 组件引用，确保与系统图标一致。

### UI 图标库

- **图标库**：Phosphor Icons (`@phosphor-icons/react`)
- **默认字重**：`weight="regular"`，活跃/选中态用 `weight="fill"`
- **尺寸 Token**：`icon-sm` (16px)、`icon-md` (20px)、`icon-lg` (24px)
- **描边一致性**：同一视觉层级内保持统一字重

### Friday 常用图标映射

| 场景 | 图标 | Phosphor Name |
|------|------|---------------|
| 终端/命令 | 🖥 | `Terminal` |
| 运行中 | ▶ | `Play` |
| 停止 | ⬛ | `Stop` |
| 成功 | ✓ | `CheckCircle` |
| 警告 | ⚠ | `Warning` |
| 错误/高风险 | ✕ | `XCircle` |
| 服务器/环境 | 🖥 | `Desktop` |
| 日志 | 📄 | `FileText` |
| 设置 | ⚙ | `Gear` |
| 会话 | 💬 | `ChatCircle` |
| 连接 | 🔗 | `Plug` |
| 复制 | ⧉ | `Copy` |
| 展开/折叠 | ▸▾ | `CaretRight` / `CaretDown` |

## 7. 数据可视化

### 7.1 实时流式图表

用于展示 jstat、GC 指标等实时采集的数据。

| 场景 | 图表类型 | 说明 |
|------|---------|------|
| GC 次数/耗时趋势 | Streaming Area Chart | 实时更新，保留最近 60-300s |
| Heap 各代占用 | 分层面积图 | Eden/Survivor/Old/Perm 分层 |
| 异常标记 | Line Chart with Highlights | 异常点用红色圆点 + 文本注释 |

**颜色约定**：
- 当前值脉冲：`#22C55E`（绿）
- 历史数据：渐变透明度（越旧越淡）
- 异常标记：`#EF4444`（红圆点 + 填充）
- 网格线：`--border` 低透明度

**无障碍**：
- 不依赖颜色 alone——异常点同时用形状（圆点）和文本注释
- 提供 Pause/Resume 按钮控制流式更新
- 键盘焦点可查看具体数据点值
- 支持 `prefers-reduced-motion`：停止流式动画，改为静态最新值

### 7.2 诊断结果摘要

用 Bullet Chart 展示关键指标 vs 健康阈值：

```
Heap 使用率    [████████████░░░░░░] 78%  阈值 85%
GC 频率       [██████░░░░░░░░░░░░] 32%  阈值 60%
线程数        [████████████████░░] 89%  阈值 90%  ⚠
```

- 正常范围：绿色条
- 接近阈值：黄色条
- 超过阈值：红色条 + ⚠ 标记

## 8. 交互模式

### 8.1 流式输出

- Agent LLM 输出逐 token 渲染（typewriter 效果），不用 spinner 等待完整响应
- 工具执行时显示 "执行中..." + spinner，完成后展示耗时
- 长时间操作（>3s）显示进度条或步骤指示器

### 8.2 进度反馈

| 操作时长 | 反馈方式 |
|---------|---------|
| < 200ms | 无反馈（感知不到延迟） |
| 200ms - 1s | Spinner / 骨架屏 |
| 1s - 3s | Spinner + 文字说明（"正在连接 192.168.1.100..."） |
| > 3s | 步骤指示器（"步骤 2/4: 正在采集 GC 数据"） |

### 8.3 动画

| 场景 | 动画 | 时长 |
|------|------|------|
| 卡片展开/折叠 | height + opacity | 200ms ease-out |
| 状态点脉冲 | opacity 0.5↔1.0 | 1.5s infinite |
| 新消息进入 | translateY(8px→0) + opacity | 250ms ease-out |
| 确认卡片出现 | scale(0.95→1) + opacity | 200ms ease-out |
| 焦点环 | box-shadow transition | 150ms |

- 所有动画尊重 `prefers-reduced-motion`：禁用动画，直接显示最终状态
### 动画时长

| Token | 值 | 用途 |
|-------|----|------|
| `--duration-fast` | 120ms | 焦点环、颜色过渡 |
| `--duration-normal` | 200ms | 卡片展开、消息进入 |
| `--duration-slow` | 350ms | 复杂过渡 |

### 缓动函数

| Token | 值 | 用途 |
|-------|----|------|
| `--ease-out` | `cubic-bezier(0.16, 1, 0.3, 1)` | 默认缓出 |
| `--ease-in-out` | `cubic-bezier(0.4, 0, 0.2, 1)` | 对称过渡 |
| `--ease-spring` | `cubic-bezier(0.34, 1.56, 0.64, 1)` | 弹性效果 |

### 8.4 键盘快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+N` | 新建诊断会话 |
| `Ctrl+K` | 快速搜索历史会话 |
| `Ctrl+Enter` | 发送补充信息 |
| `Ctrl+.` | 停止 Agent |
| `Ctrl+Shift+C` | 复制最后工具输出 |
| `Esc` | 关闭确认卡片 / 取消焦点 |

## 9. 技术实现约定

### 9.1 技术栈

| 层 | 技术 |
|----|------|
| 样式 | Tailwind CSS v4（`@theme inline` + CSS 变量） |
| 图标 | `@phosphor-icons/react` |
| 图表 | 自定义 SVG / Canvas（轻量场景）或 Recharts（复杂图表） |
| 字体 | JetBrains Mono + IBM Plex Sans (Google Fonts) |

### 9.2 主题约定

- 使用 CSS 变量定义语义色，通过 `@theme inline` 映射到 Tailwind v4
- 不在组件中硬编码颜色——所有颜色走 `bg-surface-1`、`text-muted-foreground` 等 token
- 边框使用半透明白色（`rgba(255,255,255,0.08)`），不用实色
- 滚动条、文本选中、焦点环均在 `globals.css` 全局定义

### 9.3 禁止项

- 不用 emoji 作为结构性图标（用 Phosphor SVG 图标）
- 不用 `bg-blue-500` 等硬编码颜色类
- 不用 raster PNG 图标（用 SVG）
- 不在同一视觉层级混用 filled 和 outline 图标
- 不用 `<div onClick>` 作为交互控件（用语义化 `<button>` 或 Radix primitive）

## 10. 后续演进

| 项 | v1 | 后续 |
|----|----|------|
| 主题 | Dark Mode only | 增加 Light Mode（token 已预留语义映射） |
| 图表 | 基础 streaming area + bullet | 增加更多诊断可视化（火焰图、线程状态时序图） |
| 布局 | 固定三栏 | 可拖拽调整侧栏宽度，可折叠侧栏 |
| 国际化 | 中文 | 增加英文 |
